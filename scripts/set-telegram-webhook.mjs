const base=process.env.APP_URL; const token=process.env.BOT_TOKEN; const secret=process.env.TELEGRAM_WEBHOOK_SECRET; if(!base||!token||!secret) throw new Error('APP_URL, BOT_TOKEN and TELEGRAM_WEBHOOK_SECRET required'); const r=await fetch(`https://api.telegram.org/bot${token}/setWebhook`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({url:`${base}/api/telegram`,secret_token:secret,allowed_updates:['message','callback_query']})}); console.log(await r.json());
