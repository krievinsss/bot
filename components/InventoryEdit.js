'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function InventoryEdit({id,quantity,active}){const [q,setQ]=useState(quantity);const r=useRouter();async function save(data){await fetch('/api/admin/data/inventory',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({id,...data})});r.refresh()}return <div className="flex gap-2 items-center"><input className="input w-24" type="number" min="0" value={q} onChange={e=>setQ(e.target.value)}/><button className="btn secondary" onClick={()=>save({quantity:Number(q)})}>Save</button><button className="btn secondary" onClick={()=>save({active:!active})}>{active?'Disable':'Enable'}</button></div>}
