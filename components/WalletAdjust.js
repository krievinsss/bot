'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; export default function WalletAdjust({userId}){const [open,setOpen]=useState(false),r=useRouter();if(!open)return <button className="btn secondary" onClick={()=>setOpen(true)}>Adjust</button>;return <form className="flex gap-2" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch('/api/admin/wallet/adjust',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({userId,amount:f.get('amount'),reason:f.get('reason')})});if(res.ok){setOpen(false);r.refresh()}}}><input className="input w-28" name="amount" type="number" step="0.01" required/><input className="input" name="reason" placeholder="Reason" required/><button className="btn">Apply</button></form>}
