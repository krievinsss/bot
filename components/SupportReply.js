'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; export default function SupportReply({ticketId}){const [m,setM]=useState('');const r=useRouter();return <div className="flex gap-2"><input className="input" value={m} onChange={e=>setM(e.target.value)} placeholder="Reply"/><button className="btn" onClick={async()=>{if(!m)return;await fetch('/api/admin/support/reply',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({ticketId,message:m})});setM('');r.refresh()}}>Send</button></div>}
