import test from 'node:test'; import assert from 'node:assert/strict'; const {hashPassword,verifyPassword,safeEqual}=await import('../lib/security.js'); test('password hashing',()=>{const h=hashPassword('CorrectHorseBattery!');assert.equal(verifyPassword('CorrectHorseBattery!',h),true);assert.equal(verifyPassword('wrong',h),false)}); test('safe equal',()=>{assert.equal(safeEqual('abc','abc'),true);assert.equal(safeEqual('abc','abd'),false)});
