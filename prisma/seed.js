import { PrismaClient } from '@prisma/client'; import crypto from 'crypto';
const prisma=new PrismaClient();
function hashPassword(password){const salt=crypto.randomBytes(16);const hash=crypto.scryptSync(password,salt,64,{N:16384,r:8,p:1});return `scrypt$${salt.toString('base64')}$${hash.toString('base64')}`}
async function main(){const email=(process.env.SEED_ADMIN_EMAIL||'admin@example.com').toLowerCase();const password=process.env.SEED_ADMIN_PASSWORD||'ChangeMe-Immediately-123!';await prisma.admin.upsert({where:{email},update:{},create:{email,passwordHash:hashPassword(password),role:'OWNER'}});for(const name of ['Riga','Cesis','Valmiera']) await prisma.city.upsert({where:{name},update:{},create:{name}});for(const p of [{name:'Product A',slug:'product-a',price:25},{name:'Product B',slug:'product-b',price:40},{name:'Product C',slug:'product-c',price:15}]) await prisma.product.upsert({where:{slug:p.slug},update:{},create:{...p,description:`Example description for ${p.name}.`,active:true}});await prisma.setting.upsert({where:{key:'affiliatePercent'},update:{},create:{key:'affiliatePercent',value:{percent:5}}});console.log(`Seed complete. Admin: ${email}`)}main().finally(()=>prisma.$disconnect());
