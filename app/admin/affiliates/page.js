import { prisma } from '../../../lib/db.js'; export default async function Page(){const rows=await prisma.user.findMany({where:{referralCount:{gt:0}},orderBy:{referralCount:'desc'},take:100});return <><h1 className="text-3xl font-bold mb-6">Affiliates</h1><div className="card"><table className="table"><thead><tr><th>User</th><th>Referrals</th><th>Lifetime commission</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>@{x.username||x.telegramId.toString()}</td><td>{x.referralCount}</td><td>€{Number(x.lifetimeCommission).toFixed(2)}</td></tr>)}</tbody></table></div></>}
