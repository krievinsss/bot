import { prisma } from '../../../../lib/db.js'; import SimpleCreateForm from '../../../../components/SimpleCreateForm.js'; export default async function Page(){const rows=await prisma.broadcast.findMany({orderBy:{createdAt:'desc'}});return <><h1 className="text-3xl font-bold mb-6">Broadcasts</h1><SimpleCreateForm resource="broadcasts" fields={[{name:'title',label:'Title',required:true},{name:'body',label:'Message',type:'textarea',required:true},{name:'imageUrl',label:'Image URL'},{name:'buttonText',label:'Button text'},{name:'buttonUrl',label:'Button URL'},{name:'target',label:'Target (ALL)',required:true},{name:'queue',label:'Queue immediately',type:'checkbox'}]}/><div className="card"><table className="table"><tbody>{rows.map(x=><tr key={x.id}><td>{x.title}</td><td>{x.status}</td><td>{x.sent} sent / {x.failed} failed</td></tr>)}</tbody></table></div></>}
