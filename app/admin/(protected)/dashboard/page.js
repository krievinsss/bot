import { prisma } from '../../../../lib/db.js';
export default async function Page(){ const [users,orders,payments,stock,low,revenue,recentOrders]=await Promise.all([prisma.user.count(),prisma.order.count(),prisma.payment.count({where:{status:'COMPLETED'}}),prisma.inventory.aggregate({_sum:{quantity:true},where:{active:true}}),prisma.inventory.count({where:{active:true,quantity:{lte:3}}}),prisma.order.aggregate({_sum:{price:true},where:{status:{in:['PAID','COMPLETED']}}}),prisma.order.findMany({take:8,orderBy:{createdAt:'desc'}})]); const cards=[['Revenue',`€${Number(revenue._sum.price||0).toFixed(2)}`],['Orders',orders],['Users',users],['Deposits',payments],['Inventory',stock._sum.quantity||0],['Low stock',low]]; return <><h1 className="text-3xl font-bold mb-6">Dashboard</h1><div className="grid-cards mb-8">{cards.map(([a,b])=><div className="card" key={a}><div className="muted">{a}</div><div className="text-3xl font-bold mt-2">{b}</div></div>)}</div><div className="card"><h2 className="font-bold mb-3">Recent orders</h2><table className="table"><tbody>{recentOrders.map(o=><tr key={o.id}><td>{o.publicOrderId}</td><td>{o.productNameSnapshot}</td><td>€{Number(o.price).toFixed(2)}</td><td><span className="badge">{o.status}</span></td></tr>)}</tbody></table></div></> }
