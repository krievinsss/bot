import {prisma} from '../../../../lib/db.js';import SimpleCreateForm from '../../../../components/SimpleCreateForm.js';import InventoryEdit from '../../../../components/InventoryEdit.js';import RowActions from '../../../../components/RowActions.js';import TableFilter from '../../../../components/TableFilter.js';
export default async function Page(){const [products,locations,rows]=await Promise.all([prisma.product.findMany({where:{active:true}}),prisma.location.findMany({where:{active:true},include:{city:true}}),prisma.inventory.findMany({include:{product:true,location:{include:{city:true}}},orderBy:{updatedAt:'desc'}})]);return <><h1 className="text-3xl font-bold mb-6">Inventory</h1><SimpleCreateForm resource="inventory" fields={[{name:'productId',type:'select',options:products.map(p=>({value:p.id,label:p.name})),required:true},{name:'locationId',type:'select',options:locations.map(l=>({value:l.id,label:`${l.city.name} / ${l.name}`})),required:true},{name:'quantity',label:'Quantity',type:'number',min:'0',required:true},{name:'active',label:'Active',type:'checkbox'}]}/><div className="card overflow-x-auto"><TableFilter tableId="inventory-table" placeholder="Search product, city or location…" statuses={['Active','Off','Out of stock']}/><table id="inventory-table" className="table"><thead><tr><th>Product</th><th>Location</th><th>Stock</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.map(x=>{const status=x.quantity<=0?'Out of stock':x.active?'Active':'Off';return <tr key={x.id} data-status={status}><td>{x.product.name}</td><td>{x.location.city.name} / {x.location.name}</td><td>{x.quantity}</td><td>{status}</td><td><div className="flex gap-2 items-start"><InventoryEdit id={x.id} quantity={x.quantity} active={x.active}/><RowActions resource="inventory" id={x.id} fields={[]} initial={{}}/></div></td></tr>})}</tbody></table></div></>}
