import {prisma} from '../../../../lib/db.js';import {decrypt} from '../../../../lib/encryption.js';import SimpleCreateForm from '../../../../components/SimpleCreateForm.js';import ToggleButton from '../../../../components/ToggleButton.js';import RowActions from '../../../../components/RowActions.js';import TableFilter from '../../../../components/TableFilter.js';
export default async function Page(){const[cities,rows]=await Promise.all([prisma.city.findMany({where:{active:true},orderBy:{name:'asc'}}),prisma.location.findMany({include:{city:true},orderBy:{createdAt:'desc'}})]);const cityOptions=cities.map(c=>({value:c.id,label:c.name}));return <><h1 className="text-3xl font-bold mb-6">Locations</h1><SimpleCreateForm resource="locations" fields={[{name:'cityId',label:'City',type:'select',options:cityOptions,required:true},{name:'name',label:'Internal name',required:true},{name:'publicName',label:'Public name'},{name:'privateAddress',label:'Exact private address',required:true},{name:'latitude',label:'Latitude',required:true},{name:'longitude',label:'Longitude',required:true},{name:'privateInstructions',label:'Private pickup instructions',type:'textarea'},{name:'privateImagePath',label:'Private location image',type:'image',kind:'private'},{name:'active',label:'Active',type:'checkbox'}]}/><div className="card overflow-x-auto"><TableFilter tableId="locations-table" placeholder="Search city or location…" statuses={['Active','Off']}/><table id="locations-table" className="table"><thead><tr><th>Image</th><th>City</th><th>Name</th><th>Public name</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.map(x=>{const preview=x.privateImagePath?`/api/admin/media?path=${encodeURIComponent(x.privateImagePath)}`:'';const initial={cityId:x.cityId,name:x.name,publicName:x.publicName||'',privateAddress:decrypt(x.privateAddressEnc),latitude:decrypt(x.latitudeEnc),longitude:decrypt(x.longitudeEnc),privateInstructions:decrypt(x.privateInstructionsEnc),privateImagePath:x.privateImagePath||'',active:x.active};return <tr key={x.id} data-status={x.active?'Active':'Off'}><td>{preview?<img src={preview} alt={x.name} className="h-14 w-14 rounded-lg object-cover border border-neutral-200"/>:<span className="muted text-xs">No image</span>}</td><td>{x.city.name}</td><td>{x.name}</td><td>{x.publicName||'—'}</td><td>{x.active?'Active':'Off'}</td><td><div className="flex gap-2 items-start"><ToggleButton resource="locations" id={x.id} field="active" value={!x.active} label={x.active?'Disable':'Enable'}/><RowActions resource="locations" id={x.id} initial={initial} fields={[{name:'cityId',label:'City',type:'select',options:cityOptions},{name:'name',label:'Internal name'},{name:'publicName',label:'Public name'},{name:'privateAddress',label:'Exact private address'},{name:'latitude',label:'Latitude'},{name:'longitude',label:'Longitude'},{name:'privateInstructions',label:'Private pickup instructions',type:'textarea'},{name:'privateImagePath',label:'Private location image',type:'image',kind:'private',preview},{name:'active',label:'Active',type:'checkbox'}]}/></div></td></tr>})}</tbody></table></div></>}
