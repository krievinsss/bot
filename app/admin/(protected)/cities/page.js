import { prisma } from '../../../../lib/db.js'; import SimpleCreateForm from '../../../../components/SimpleCreateForm.js'; import ToggleButton from '../../../../components/ToggleButton.js';
export default async function Page(){const rows=await prisma.city.findMany({orderBy:{name:'asc'}});return <><h1 className="text-3xl font-bold mb-6">Cities</h1><SimpleCreateForm resource="cities" fields={[{name:'name',label:'City name',required:true},{name:'active',label:'Active',type:'checkbox'}]}/><div className="card"><table className="table"><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.active?'Active':'Off'}</td><td><ToggleButton resource="cities" id={x.id} field="active" value={!x.active} label={x.active?'Disable':'Enable'}/></td></tr>)}</tbody></table></div></>}
