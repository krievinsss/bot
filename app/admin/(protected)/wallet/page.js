import { prisma } from '../../../../lib/db.js'; export default async function Page(){const rows=await prisma.walletTransaction.findMany({include:{user:true},orderBy:{createdAt:'desc'},take:300});return <><h1 className="text-3xl font-bold mb-6">Wallet Ledger</h1><div className="card"><table className="table"><thead><tr><th>User</th><th>Type</th><th>Amount</th><th>Before</th><th>After</th><th>Reference</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>@{x.user.username||'—'}</td><td>{x.type}</td><td>{Number(x.amount).toFixed(2)}</td><td>{Number(x.balanceBefore).toFixed(2)}</td><td>{Number(x.balanceAfter).toFixed(2)}</td><td>{x.referenceId||x.reason||'—'}</td></tr>)}</tbody></table></div></>}
